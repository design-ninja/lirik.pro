<div class='infotable'>
  <div class='infotable__row'>
    <span class='infotable__row-label'>Сейчас я здесь</span> <a class='infotable__row-value' href='https://www.google.com/maps/place/Пхукет' target='_blank' rel='noopener' id='location'>—</a>
  </div>
  <div class='infotable__row'>
    <span class='infotable__row-label'>Слушаю</span> <a class='infotable__row-value' href='#' target='_blank' rel='noopener' id='spotify'>—</a>
  </div>
  <div class='infotable__row'>
    <span class='infotable__row-label'>Кодю</span> <a class='infotable__row-value' href='#' target='_blank' rel='noopener' id='wakatime'>—</a>
  </div>
  <div class='infotable__row'>
    <span class='infotable__row-label'>Читаю</span> <a class='infotable__row-value' href='https://www.amazon.com/Creative-Act-Way-Being/dp/0593652886' target='_blank' rel='noopener' id='book'>The Creative Act: A Way of Being</a>
  </div>
  <div class='infotable__row'>
    <span class='infotable__row-label'>Учу</span> <a class='infotable__row-value' href='https://www.youtube.com/@letslearnthai' target='_blank' rel='noopener' id='learn'>Тайский язык</a>
  </div>
</div>

<style lang='scss'>
  .infotable {
    display: flex;
    flex-direction: column;
    gap: var(--space-8);
    border-radius: var(--radius-m);
    border: var(--size-1) solid var(--color-border-primary);
    padding: var(--space-20);

    &__row {
      display: flex;
      align-items: center;
      gap: var(--space-16);
      justify-content: space-between;
      opacity: 0.7;
      font-family: monospace;
      font-size: var(--font-size-14);
      border-bottom: var(--size-1) solid var(--color-border-primary);
      padding-bottom: var(--space-8);

      &:last-child {
        border-bottom: none;
        padding-bottom: 0;
      }

      @media screen and (max-width: 768px) {
        flex-direction: column;
        align-items: flex-start;
        gap: var(--space-2);
        padding-bottom: var(--space-12);
      }
    }
    &__row-label {
      flex: 0 0 auto;
    }
    &__row-value {
      width: fit-content;
      min-width: 0;
      display: inline-block;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
      text-align: right;
    }
  }
</style>

<script>
  async function loadLocation() {
    const el = document.getElementById('location') as HTMLAnchorElement | null;
    if (!el) return;

    const RAW_URL = import.meta.env.PUBLIC_LOCATION_JSON_URL;

    if (!RAW_URL) {
      el.textContent = '—';
      return;
    }

    try {
      const url = RAW_URL + (RAW_URL.includes('?') ? '&' : '?') + 't=' + Date.now();
      const res = await fetch(url, { cache: 'no-store' });
      if (!res.ok) throw new Error('bad status');
      const data = await res.json();
      const value = [data.city, data.country].filter(Boolean).join(', ');
      el.textContent = value || '—';
      if (value) {
        const query = encodeURIComponent(value);
        el.href = `https://www.google.com/maps/place/${query}`;
      } else {
        el.href = '#';
      }
    } catch {
      el.textContent = '—';
    }
  }

  async function loadSpotify() {
    const el = document.getElementById('spotify') as HTMLAnchorElement | null;
    if (!el) return;

    try {
      const res = await fetch('/api/spotify-last.json', { cache: 'no-store' });
      if (!res.ok) throw new Error('bad status');
      const data = await res.json();
      if (data?.url && data?.title) {
        el.href = data.url;
        el.textContent = data.title;
      } else {
        el.textContent = '—';
      }
    } catch {
      el.textContent = '—';
    }
  }

  async function loadWakatime() {
    const el = document.getElementById('wakatime') as HTMLAnchorElement | null;
    if (!el) return;

    try {
      const res = await fetch('/api/wakatime.json?days=7', { cache: 'no-store' });
      if (!res.ok) throw new Error('bad status');
      const data = await res.json();
      if (data?.totalText) {
        el.textContent = `${data.totalText} / 7 days`;
        el.href = 'https://wakatime.com/@lirik';
      } else {
        el.textContent = '—';
      }
    } catch {
      el.textContent = '—';
    }
  }

  // Load on initial page load and on every navigation (View Transitions)
  document.addEventListener('astro:page-load', () => {
    loadLocation();
    loadSpotify();
    loadWakatime();
  });
</script>


